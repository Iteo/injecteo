name: injecteo_annotation
description: A new Flutter package project.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  analyzer: ^5.2.0
  injecteo:
    path: ../injecteo

dev_dependencies:
  lint: ^1.10.0
  test: ^1.21.6
